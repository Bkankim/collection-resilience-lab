# 계획

제한 시간 안에 무엇까지 만들고 무엇을 안 만들지 먼저 정한다. 범위를 안 정하면
절반쯤 만든 것이 여러 개 남는다.

## 무엇을 증명하려는가

수집이 막혔을 때 **무엇 때문에 막혔는지 구분하고, 구분에 따라 다르게 대응하는 것**을
실제로 동작하는 코드와 측정된 수치로 보인다.

막힘을 뭉뚱그리면 대응은 재시도 하나뿐이다. 속도 제한, 출발지 차단, 세션 만료,
응답 구조 변경은 각각 다른 대응을 요구하고, 잘못 대응하면 상황이 나빠진다.
예를 들어 속도 제한에 워커마다 개별 백오프를 걸면 워커 수만큼 한도를 초과한다.

## 왜 대상 서버를 직접 만드는가

외부 사이트를 실험 대상으로 쓰면 약관과 법을 건드린다. 그리고 상대의 차단 조건을
모르니 측정이 재현되지 않는다.

대상 서버를 직접 만들면 차단 조건을 스위치로 켜고 끌 수 있다. 같은 조건에서 반복
측정이 되고, 차단하는 쪽의 임계치와 오탐도 함께 설계하게 된다.

## 범위

### 만든다

- 수집 대상 서버: 로그인, 2차 인증, 거래내역 조회, 차단 3종 토글
- 수집 클라이언트: 세션 관리, 응답 파싱, 실패 분류
- 수집 요청 API: 요청을 받아 큐에 넣고 작업 ID를 즉시 반환
- 큐 워커: 멱등 처리, 재시도, 출발지 전환, DLQ
- 차단 시나리오 측정과 결과표
- 런타임 3종(Node 22/24/26) 검증. 22는 22.19.0 이상(undici 8의 요구)이고, CI는 하한 22.19.0을 따로 돈다

### 만들지 않는다

| 항목 | 이유 |
|---|---|
| 공동인증서 기반 전자서명 | 2차 인증은 TOTP로 단순화. 모사하면 어휘만 흉내내게 된다 |
| 운영 대시보드 | 측정 결과는 스크립트 출력과 결과표로 충분하다 |
| SDK 패키지 분리 | 소비자가 하나뿐인 상태에서 나누면 구조만 늘어난다 |
| LLM 기반 실패 분류 | 분류 규칙이 일곱 가지뿐이라 규칙으로 충분하다 |
| 단일 실행파일 빌드 | 배포 대상이 없다 |
| 응답 구조 변경 자동 대응 | 구조가 바뀌면 코드를 고쳐야 풀린다. 다른 파서로 다시 읽는 자동 대응은 문제를 숨긴다. 원본을 남기고 사람에게 넘긴다 |

만들지 않기로 한 것은 못 하는 것과 다르다. 지금 만들면 무엇이 늘어나는지 적어 뒀다.

## 기술 선택

구현에 들어가기 전에 정해 둔다. 작업 중에 고르면 고르는 데 시간을 쓴다.

| 자리 | 선택 | 이유 |
|---|---|---|
| 큐 | BullMQ | `worker.rateLimit()`이 "큐 전체를 멈춘다"는 대응과 그대로 맞고, 이때 작업의 시도 횟수를 깎지 않는다. 속도 제한 때문에 멀쩡한 작업이 DLQ로 가는 것을 구현으로 막아 준다 |
| 출발지 | tinyproxy 컨테이너 2개 + `undici.ProxyAgent` | 출발지를 실제로 분리해야 차단이 의미를 갖는다. undici는 연결 풀이 출발지별로 유지되므로 전환할 때 dispatcher를 바꿔야 한다 |
| 2차 인증 | otplib | TOTP 생성과 검증을 양쪽에서 쓴다 |
| 인코딩 | iconv-lite | Node 내장 `TextEncoder`는 `euc-kr` 레이블을 받아도 무시하고 UTF-8을 낸다. 조용히 틀린 바이트가 나가므로 별도 라이브러리가 필요하다 |
| HTTP 서버 | Fastify | 차단 조건을 `onRequest` 훅 한 자리에 모을 수 있다 |

## 진행

마일스톤마다 나가는 조건(Exit)을 먼저 적고, 그 조건을 검증 가능한 형태로만 쓴다.
"동작한다"가 아니라 "무엇을 실행하면 무엇이 나온다"로 적는다.

| 마일스톤 | 나가는 조건 |
|---|---|
| M1 수집 대상 서버 | 로그인부터 거래내역까지 한 번에 통과. 차단 3종을 관리 API로 켜고 끈 결과가 응답 코드로 확인됨 |
| M2 수집 파이프라인 | 세션을 유지하며 100건 수집·파싱. 큐에 200건을 넣고 워커가 나눠 처리. 같은 작업을 두 번 넣어도 결과가 한 번만 쌓임 |
| M3 차단 대응과 측정 | 차단을 켜면 성공률이 떨어지고 대응 후 회복되는 과정이 수치로 남음. 테스트와 CI 통과 |
| M4 공개 준비 | README에 측정 결과표, 막혔던 기록 3건, 실행 절차 |

## 기록 규칙

- 막혔던 것은 `docs/TROUBLESHOOTING.md`에 증상 → 가설 → 증거 → 원인 → 수정 순으로 남긴다.
  해결만 적으면 나중에 같은 곳에서 또 막힌다.
- 수치는 측정 조건과 함께 적는다. 조건 없는 수치는 비교할 수 없다.
- 안 한 것은 안 했다고 적는다. 커밋의 `Not-tested:` trailer와 위 "만들지 않는다" 표가 그 자리다.
